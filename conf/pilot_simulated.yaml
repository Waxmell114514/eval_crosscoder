defaults:
  - task: json_only
  - model: simulated_medium
  - data: pilot_generated
  - lora: simulated_default
  - cache: default
  - methods: pilot
  - evaluation: pilot
  - causal: default
  - reporting: default
  - _self_

experiment_name: pilot_json_only
backend: simulated
runs_root: runs
